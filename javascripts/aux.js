let getRandomNumber = limit => {
    return Math.floor(Math.random() * limit);
}

let getDistance = (click, target) => {
    let diffx = click.offsetX - target.x;
    let diffy = click.offsetY - target.y;
    return Math.sqrt(Math.pow)
}